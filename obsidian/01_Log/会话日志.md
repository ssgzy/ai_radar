# 会话日志

关联：[[项目总览]] | [[当前状态]] | [[任务笔记 - AI Radar 搭建]] | [[版本迭代记录]] | [[问题与坑点]]

## 2026-03-10

### 01:39-01:53 初始核查

- 确认当前工作目录是 `/Users/sam/Documents/AI Models`
- 确认该目录可用，且存在 `.obsidian`
- 发现已有 `/Users/sam/Documents/AI Models/ai_radar` 目录
- 检查发现当前目录不是 Git 仓库

### 01:53 当前决定

- 选择 `/Users/sam/Documents/AI Models/ai_radar` 作为项目代码根目录
- 选择 `/Users/sam/Documents/AI Models/ai_radar/obsidian` 作为项目笔记区
- 先初始化文档与目录，再做 V1 代码重构

### 已读取内容

- 现有 `src/main.py`
- 现有 `src/collectors/arxiv_collector.py`
- 现有 `src/collectors/github_collector.py`
- 现有 `src/agents/summarizer.py`
- 现有 `requirements.txt`

### 初步发现

- 现有 collector 逻辑是单脚本风格，可作为参考，但结构不满足目标蓝图
- 现有 summarizer 把模型和请求参数写死，不符合“模型策略模块化”的要求
- 当前目录结构明显不完整，缺失配置层、导出层、状态层、Obsidian 导出层和测试层

### 01:53-02:10 结构化重构

- 创建 `config/`、`docs/`、`obsidian/`、`outputs/`、`data/`、`src/`、`tests/`、`.runtime/` 等目录骨架
- 创建 `README.md`、`pyproject.toml`、`.env.example`、`.gitignore`
- 新建 `run_once.py`、`run_manual.py`、`run_ai_radar.py`
- 重构 `src/collectors/`、`src/agents/`、`src/exporters/`、`src/utils/`、`src/pipeline.py`、`src/cli.py`
- 新增 `scripts/test_ollama.py`
- 新增基础测试 `tests/test_summarizer.py`、`tests/test_pipeline.py`

### 02:10-02:14 运行前验证

- 运行 `python -m compileall config src scripts tests run_once.py run_manual.py run_ai_radar.py`
- 首次运行 `python -m pytest -q` 发现本机缺少 `pytest`
- 运行 `python -m pip install -r requirements.txt` 补齐测试依赖
- 再次运行 `python -m pytest -q`，结果为 `2 passed`
- 运行 `python scripts/test_ollama.py` 验证本地模型服务

### 02:14-02:16 第一轮真实运行

- 执行命令：`python run_once.py --max-items 1`
- 运行 ID：`20260310_021418`
- 首次真实跑通 `arXiv + GitHub + Ollama + JSON + Obsidian`
- 发现两个可读性问题：
  - Obsidian 基本信息列表出现 `- -`
  - 部分模型输出带有 `**` 等 Markdown 噪音

### 02:16-02:17 质量修正与第二轮基线运行

- 调整默认模型为 `mistral:7b`
- 增加 Ollama 心跳式等待提示
- 清理模型输出中的 Markdown 粗体噪音
- GitHub collector 在无描述场景下补充仓库元信息
- 再次执行 `python run_once.py --max-items 1`
- 运行 ID：`20260310_021607`
- 产出新的 V1 基线结果，写入 [[结果索引]]

### 02:17 进入 V2

- 明确本轮 V2 范围：
  - 增加 `Scorer`
  - 增加 `Tagger`
  - 改进 exporter 的 Markdown 结构和排序
  - 增强运行记录，加入阶段统计和耗时
- 准备先更新 Obsidian 计划，再开始代码修改

### 02:18-02:48 V2 实现

- 更新 `config/scoring.yaml`，加入权重、阈值、兴趣关键词和标签规则
- 扩展 `ProcessedItem`，新增 `tags`、`score_breakdown`、`total_score`、`priority_level`、`recommendation_cn`、`score_reason_cn`
- 扩展 `SourceRunRecord`，新增开始时间、结束时间、耗时和阶段统计
- 新增 `src/agents/tagger.py`
- 新增 `src/agents/scorer.py`
- 改造 `src/pipeline.py`，让流程变为 `collect -> summarize -> tag -> score -> export`
- 改造 `src/exporters/obsidian_exporter.py`，让日报按总分排序，并给条目笔记增加 V2 结论、标签和分数拆解
- 改造 `src/exporters/json_exporter.py`，增加 `scored JSON`
- 改造 `src/utils/logger.py`，增加 `scorer.log` 和 `tagger.log`
- 新增测试 `tests/test_tagger.py`、`tests/test_scorer.py`

### 02:48-02:50 V2 真实运行

- 执行命令：`python run_once.py --max-items 2`
- 运行 ID：`20260310_024851`
- 处理结果：`arXiv` 2 条，`GitHub` 2 条，总计 4 条
- 总耗时：约 62.4 秒
- 生成：
  - `merged_20260310_024851.json`
  - `scored_20260310_024851.json`
  - V2 日报 note
  - 4 条带标签和分数的条目笔记

### 02:50 进入 V3

- 明确本轮 V3 范围：
  - 接入 `RSS`
  - 接入 `Hacker News`
  - 接入 `News`
  - 实现 merge / dedupe
- 验证默认真实数据源可用性：
  - `https://huggingface.co/blog/feed.xml`
  - `https://openai.com/news/rss.xml`
  - `https://www.marktechpost.com/feed/`
  - `https://techcrunch.com/category/artificial-intelligence/feed/`
  - `https://hacker-news.firebaseio.com/v0/topstories.json`
