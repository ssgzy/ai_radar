# 常用命令

关联：[[项目总览]] | [[任务笔记 - AI Radar 搭建]] | [[结果索引]] | [[自动运行方案]]

## 环境检查

```bash
pwd
ls -la
```

## 查看项目结构

```bash
find /Users/sam/Documents/AI\ Models/ai_radar -maxdepth 3 -type f | sort
```

## 计划中的运行命令

以下命令已经在本轮使用或验证：

```bash
python -m pip install -r requirements.txt
python -m pytest -q
python scripts/healthcheck_ollama.py
python scripts/generate_scheduler_assets.py --hour 9 --minute 0
python run_once.py
python run_once.py --max-items 1
python run_once.py --max-items 2
python run_manual.py --sources arxiv github --max-items 2
python run_manual.py --sources arxiv github rss hackernews news --max-items 1
python run_ai_radar.py --max-items 1
```

## 模型切换

```bash
OLLAMA_MODEL=qwen3.5:9b python run_once.py --max-items 1
OLLAMA_MODEL=deepseek-r1:8b python run_once.py --max-items 1
```

## 常用调试命令

```bash
python run_once.py --max-items 1
python run_manual.py --sources rss hackernews news --max-items 1
python run_ai_radar.py --max-items 1
./scripts/run_scheduler_once.sh --max-items 1
python -m pytest -q
find outputs/obsidian/dashboards/daily -maxdepth 2 -type f | sort
find outputs/reports/weekly -maxdepth 2 -type f | sort
find outputs/debug/failed_items -maxdepth 2 -type f | sort
```

## 说明

- 这里只记录高频命令
- 更细的运行记录会写入 [[会话日志]]
